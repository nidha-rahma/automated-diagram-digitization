server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # All routes fallback to index.html for React Router (SPA)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
